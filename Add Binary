class Solution:
    def addBinary(self, a: str, b: str) -> str:
        sum_decimal = int(a, 2) + int(b, 2)
        return bin(sum_decimal)[2:] #remove 0b prefix
'''first convert into decimal, add, again convert into binary
bin(10) = 0b1010 so remove prefix 0b'''
